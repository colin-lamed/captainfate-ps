{
  "name": "captain-fate",
  "version": "0.1.0",
  "description": "Captain Fate (Purescript)",
  "main": "",
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp browserify --to dist/Main.js",
    "build-fail-on-warn": "pulp browserify --to dist/Main.js -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript-psa": "^0.5.0-rc.1",
    "rimraf": "^2.6.1"
  },
  "dependencies": {
    "create-react-class": "^15.6.3",
    "react": "^0.14.8",
    "react-dom": "^0.14.6"
  }
}
